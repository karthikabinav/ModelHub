from setuptools import find_packages, setup

setup(
    name="modelhub",
    version="0.0.1",
    description="ModelHub - framework for LLM research",
    packages=find_packages(),
    python_requires=">=3.10",
    install_requires=[],
)
